# HiAgent 完成情况

> 状态：**v1 已建**。最近更新：2026-07-27。
> 由旧 HiplayAgent（按模块组织）重构为按能力组织。对应 Hiplay 计划的 P0 全部完成。

## .claude/ 配置

| 项 | 状态 | 说明 |
|---|---|---|
| 11 subagent | ✅ 已写 | 4 共享 + 3 wiki 生产者 + 4 feature 流水线 |
| 9 workflow | ✅ 已写 | entry + 8 用例（纯编排） |
| `settings.json` + `CLAUDE.md` + `README.md` + `方案设计.md` | ✅ 已写 | CLAUDE.md 含共享 wiki 约定；agent 含 CRG MCP 工具对照表 |

## 重构要点

- **能力与用例分离**：code-graph/wiki-reader/code-tracer/log-parser 跨用例复用
- **合并重复**：code-tracer+bug-tracer→code-tracer；cartographer→code-graph；wiki-reader 统一
- **wiki 生产者家族**：共享约定（CLAUDE.md），内容各异
- **feature-planner/coder/reviewer/tester 加 `feature-` 前缀**
- **CRG 全命令面入 code-graph.md**（20+ CLI 子命令 + 30 MCP 工具）；6 个 CRG 用户 agent 各加「CRG MCP 工具」段

## tools/

| 项 | 状态 | 说明 |
|---|---|---|
| logscope-triage CLI | ✅ 已带 | `tools/src/logscope_triage/` + `pyproject.toml`；22 单测过 |
| 鸿蒙 parser（hilog/HiSysEvent/faultlog） | ✅ | --json/--log-format/__version__ 已实现 |

## 计划优先级

对应 Hiplay 计划（《Hiplay Agent 开发计划与进展》的计划优先级段），外网可做的 P0 已全部完成：

| 优先级 | # | 任务 | 开发地 | 验收 | 状态 |
|---|---|---|---|---|---|
| P0 | 1 | 解耦重构（能力层 + 用例编排层） | 外网 | 11 agent + 9 workflow 已写完 | ✅ 完成 |
| P0 | 2 | CRG 全命令面 + MCP 工具对照入 agent | 外网 | code-graph.md + 6 个 CRG 用户 agent 各加 MCP 段 | ✅ 完成 |
| P0 | 3 | LogScope 日志解析（Hilog + 崩溃日志） | 外网 | 22 单测过，--json/--log-format 已实现 | ✅ 完成 |
| P1 | 4 | 质量门禁（每 workflow 加 verify phase） | 外网写 + 内网验 | 输出带「验证通过/失败」 | ⏳ |
| P1 | 5 | 经验管理接雅典娜知识库 | 内网（需雅典娜） | 新 case RAG 命中旧经验 | ⏳ |
| P1 | 6 | 代码开发 + PR 流程接入（feature-implement workflow） | 内网 + AI 账号 | feature 端到端产 PR | ⏳ |
| P1 | 9 | 经验库质量门 + 过期检测 | 外网 | 归档 critic 门（只存 high）+ 检索时 git diff 验过期 + 证据链进 frontmatter | ✅ 完成 |
| P2 | 7 | 加业务 HiSysEvent 事件目录 + 真实长日志端到端 + native 栈符号化 | 内网 + AI 账号 | 真实日志定位到代码行 | ⏳ |
| P2 | 8 | entry 路由 + diag/bug-trace/wiki 生成端到端验证 | 内网 + AI 账号 | 模糊 prompt 分到对模块 | ⏳ |

> 排序依据：**P0 全部外网可做**（业务无关，传回内网即可用）——现已全部完成；**P1 与 P2 的先后可根据情况决定**——P2（内网补业务）依赖 AI 账号下发，账号未下时可先做 P1 扩展中可外网编码的部分（如质量门禁）；雅典娜知识库另需雅典娜访问权限。

## 待办

- [ ] P1.4 质量门禁 verify phase
- [ ] P1.5 雅典娜知识库接入
- [ ] P1.6 feature-implement workflow（coder→reviewer→tester 循环）+ PR 流程
- [x] P1.9 经验库质量门 + 过期检测（归档 critic 门只存 high + 检索 git diff 验过期 + 证据链进 frontmatter）
- [ ] P2.7 业务 HiSysEvent 事件目录 + 真实长日志端到端 + native 栈符号化
- [ ] P2.8 entry 路由 + diag/bug-trace/wiki 生成端到端验证
