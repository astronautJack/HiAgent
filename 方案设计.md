# HiAgent 方案设计

> **HiAgent** — 解耦版 agent 工具集。按能力层 + 用例编排层组织。
> 状态：**v1 已建**——11 subagent + 9 workflow + Python CLI + 配置/文档。对应 Hiplay 计划 P0 全部完成。

## 设计基线

- **能力与用例分离**：可复用能力（code-graph/wiki-reader/code-tracer/log-parser）抽成共享 subagent；用例 workflow 纯编排，不含可复用逻辑。
- **wiki 生产者家族共享机制**：arch/flow/exp-writer 遵循共享 wiki 约定（frontmatter、增量刷新、索引格式，写进 AGENTS.md 一处），内容模板各异。
- **CRG 全命令面集中参考**：code-graph.md 含 20+ CLI 子命令 + 30 MCP 工具对照；6 个 CRG 用户 agent 各有「CRG MCP 工具」段（MCP 首选 + Bash 兜底）。
- **人在 loop 是 workflow 边界**：design/bug-trace/diag 返回报告后会话交人审。
- **不自动 commit/push**。

## 解耦带来的收益

1. 加新 wiki 类型只加一个 producer（如「API 契约 wiki」），不动 code-graph/wiki-reader/任何 workflow。
2. 加新定位用例只编排现有能力（如「性能回退定位」= wiki-reader + code-tracer 换性能指标当症状）。
3. CRG 升级/替换只改 code-graph 一处（建图+状态+新鲜度集中）。

## Agent 拓扑

| 层 | agent | tools | 职责 |
|---|---|---|---|
| 共享 | code-graph | Read, Bash, Glob | CRG build/status/freshness/visualize/wiki 子命令；全命令面参考 |
| 共享 | wiki-reader | Read, Grep, Bash, Glob | 读索引+匹配+按需取页 |
| 共享 | code-tracer | Read, Grep, Bash, Glob | 反向回溯定位根因（输入=症状） |
| 共享 | log-parser | Read, Write, Bash | logscope-triage CLI 包装 |
| 专职 | arch-writer | Read, Write, Bash, Grep, Glob | 架构文档 + 结构页同步 |
| 专职 | flow-writer | Read, Write, Bash, Grep, Glob | 业务流页 + error_index |
| 专职 | exp-writer | Read, Write, Grep, Bash, Glob | 经验案例页 + 索引 |
| 专职 | feature-planner | Read, Grep, Bash, Glob | 需求→设计 |
| 专职 | feature-coder | Read, Write, Edit, Bash, Grep, Glob | 实现 |
| 专职 | feature-reviewer | Read, Grep, Bash, Glob | 自审 + 影响面 |
| 专职 | feature-tester | Read, Bash, Glob | 门禁 |

## 合并记录（从旧 HiplayAgent 12 → 11，消掉所有重复逻辑）

- cartographer → 并入 code-graph
- logscope-code-tracer + codewiki-bug-tracer → 合并 code-tracer（输入泛化为「症状」）
- logscope-wiki-reader + codewiki 读 wiki 逻辑 → 统一 wiki-reader
- 三个 wiki-writer 保持独立（内容不同），但共享 AGENTS.md 约定

## 配置

`.cac/settings.json`：合并 bash 权限 + CRG MCP server（`uvx code-review-graph mcp`）。deny 优先。

## 计划优先级

对应 Hiplay 计划（《Hiplay Agent 开发计划与进展》的计划优先级段）：

| 优先级 | # | 任务 | 开发地 | 验收 | 状态 |
|---|---|---|---|---|---|
| P0 | 1 | 解耦重构（能力层 + 用例编排层） | 外网 | 11 agent + 9 workflow 已写完 | ✅ 完成 |
| P0 | 2 | CRG 全命令面 + MCP 工具对照入 agent | 外网 | code-graph.md + 6 个 CRG 用户 agent 各加 MCP 段 | ✅ 完成 |
| P0 | 3 | LogScope 日志解析（Hilog + 崩溃日志） | 外网 | 22 单测过，--json/--log-format 已实现 | ✅ 完成 |
| P1 | 4 | 质量门禁（每 workflow 加 verify phase） | 外网写 + 内网验 | 输出带「验证通过/失败」 | ⏳ |
| P1 | 5 | 经验管理接雅典娜知识库 | 内网（需雅典娜） | 新 case RAG 命中旧经验 | ⏳ |
| P1 | 6 | 代码开发 + PR 流程接入（feature-implement workflow） | 内网 + AI 账号 | feature 端到端产 PR | ⏳ |
| P2 | 7 | 加业务 HiSysEvent 事件目录 + 真实长日志端到端 + native 栈符号化 | 内网 + AI 账号 | 真实日志定位到代码行 | ⏳ |
| P2 | 8 | entry 路由 + diag/bug-trace/wiki 生成端到端验证 | 内网 + AI 账号 | 模糊 prompt 分到对模块 | ⏳ |

> 排序依据：**P0 全部外网可做**（业务无关，传回内网即可用）——现已全部完成；**P1 与 P2 的先后可根据情况决定**——P2（内网补业务）依赖 AI 账号下发，账号未下时可先做 P1 扩展中可外网编码的部分（如质量门禁）；雅典娜知识库另需雅典娜访问权限。
